# Demo evaluation metric

1. install dependencies:

    `$pip install -r requirements.txt`

2. download tiny coco dataset:

    `$git clone https://github.com/dataloop-ai/tiny_coco.git`

3. run validation:

    `$python val.py`
